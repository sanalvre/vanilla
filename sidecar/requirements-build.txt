# Core dependencies for PyInstaller sidecar build.
# Agents/ingestion (crewai, marker-pdf) are imported lazily at runtime
# so they don't need to be present for the base build.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
sqlalchemy>=2.0
aiofiles>=23.0
httpx>=0.27
pyinstaller>=6.6
